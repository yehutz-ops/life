# מפרט עיצוב — מסך הבית של אזור "עבודה" (`/work`)

מסמך זה מתאר **בדיוק** את מה שקיים היום במסך, כדי שאפשר יהיה להעביר אותו כקלט מלא למעצב/מודל
ולקבל עיצוב חדש בלי גישה לקוד. כל המחרוזות, הצבעים והמידות מועתקים מהמימוש בפועל.

---

## 1. הקשר המוצר

**Life Control Center** — אפליקציית ווב אישית (SPA) לניהול כל תחומי החיים במקום אחד.
עברית מלאה, RTL, משתמש יחיד, אחסון מקומי בדפדפן (IndexedDB), ללא שרת.

התחומים: עבודה · לימודים · אישי · בית · בריאות וספורט · כספים · פיתוח אישי ויוזמות.

**המסך המדובר:** דף הכניסה לתחום "עבודה" — מסך Hub שממנו מתפצלים ל-6 תת-מערכות.
זה לא דף רשימת משימות; זה **לוח כניסה** שאמור לענות בשנייה על "מה דחוף עכשיו" ו"לאן אני נכנס".

**המשתמש:** מנהל שיווק/תוכן שעובד מול מותגים, משפיענים, קמפיינים ממומנים ומשלוחי יבוא.

---

## 2. שפת עיצוב קיימת (חובה לשמור)

### פונטים
| שימוש | פונט |
|---|---|
| כל הטקסט | **Rubik** (400 / 500 / 600 / 700), fallback: `Arial Hebrew`, system-ui |
| לוגו בלבד | `New Peninim MT`, Georgia (סריף — מחלקה `.font-elegant`) |

### צבעים (ערכי אמת מהקוד)
| תפקיד | Light | Dark |
|---|---|---|
| רקע העמוד | `#F7F5F1` (off-white חמים) | `#1C1A18` |
| משטח (כרטיס/פאנל) | `#FFFFFF` | `stone-900` `#1C1917` |
| גבול | `stone-200/60-70` | `stone-800` |
| טקסט ראשי (charcoal) | `stone-800/900` | `stone-100` |
| טקסט משני | `stone-500` | `stone-400` |
| טקסט שלישוני / placeholder | `stone-400` | `stone-500` |
| **Accent ראשי** | **`amber-800` = `#92400E`** | `amber-400` לטקסט על כהה |
| רקע Accent עדין | `amber-50` | `amber-950` |

> **חשוב:** ה-accent של כל האתר הוא החום-כתום `#92400E`. אין להחליף אותו בכתום בהיר.
> כל ששת כרטיסי הקטגוריה משתמשים באותו accent אחד (`SITE_ACCENT_HEX`) — במכוון, לאחידות.

### רדיוסים
| אלמנט | רדיוס |
|---|---|
| כרטיס/פאנל גדול, כרטיס קטגוריה, שורת חיפוש | `rounded-3xl` = **24px** |
| כפתור, שדה קלט, תא ביומן | `rounded-xl` / `rounded-lg` = 12px / 8px |
| כפתורי אייקון עגולים, נקודות, "צ'יפים" | `rounded-full` |

### צללים ומרווחים
- צל: `shadow-sm` בלבד; `hover:shadow-md`. הכל עדין מאוד — אין צללים כבדים.
- מרווח בין סקשנים: `space-y-8` (32px).
- מרווח גריד: `gap-3 sm:gap-4` (12px / 16px).
- Padding פנימי של פאנל: 20px (`p-5`).

### אנימציות
- `hover:scale-105` על תמונת כרטיס, 300ms.
- כפתורי פעולה מופיעים ב-hover בלבד (`opacity-0 group-hover:opacity-100`).
- קיימת אנימציית כניסה `fade-in-blur` (0.5s, opacity+translateY(6px)+blur) — מכבדת `prefers-reduced-motion`.

---

## 3. שלד המסך (Shell) — משותף לכל הדפים

```
┌──────────────────────────────── מסך ────────────────────────────────┐
│  [ תוכן ראשי — max-width 1024px, ממורכז ]      │  [ Sidebar — ימין ] │
└─────────────────────────────────────────────────────────────────────┘
```

**Sidebar (בצד ימין, כי RTL):**
- רוחב 256px (מכווץ: 80px). רקע לבן 80% + `backdrop-blur`. גבול פנימי דק.
- למעלה: "Life Control Center" (סריף, 18px, bold) + מתחת "מרכז שליטה אישי" (12px, אפור).
- כפתור כיווץ `«` / `»`.
- ניווט (6 קישורים, עם אימוג'י): 🏠 בית · 🗓️ יומן · ✅ משימות · 📁 פרויקטים · 🔍 חיפוש · ⚙️ הגדרות
- קישור פעיל: רקע `amber-50`, טקסט `amber-900`.
- בתחתית: "גרסת תצוגה — נתוני דוגמה" (12px, `stone-300`).

**אזור התוכן:**
- `max-width: 1024px`, ממורכז, padding 16px (מובייל) / 24px / 40px (דסקטופ).
- בראש התוכן, בפינה **השמאלית**: כפתור מצב תצוגה (☀️/🌙).
- **מובייל:** ה-Sidebar נעלם, במקומו Bottom Tab Bar קבוע + כפתור צף עגול `+` בפינה השמאלית-תחתונה.

---

## 4. תוכן הדף — סקשן אחר סקשן (מלמעלה למטה)

### 4.1 כותרת הדף
שורה אחת. מימין לשמאל בסדר הזה:

1. כפתור חזרה `←` — ריבוע 36×36, `rounded-xl`, גבול אפור, מוביל ל-`/` (דף הבית)
2. אייקון תיק עבודה (קווי, 24×24, `stroke-currentColor`, עובי 1.6)
3. הכותרת **"עבודה"** — 24px, `font-bold`, charcoal

*(אין שדה חיפוש בדף הזה כרגע — בניגוד לדף "בית" שיש בו.)*

### 4.2 שורת קליטה מהירה — QuickCaptureBar
כרטיס לבן `rounded-3xl` עם גבול וצל עדין, **בנוי משתי שורות**:

- **שורה עליונה:** שדה טקסט ברוחב מלא, 16px, placeholder:
  `"כתוב או אמור משהו שצריך לזכור..."`
- **שורה תחתונה:** רקע `stone-50/70`, גבול עליון דק, 8px padding אנכי:
  - **בצד ימין:** 🎤 כפתור הקלטה (עיגול 36×36) + קבוצת מתג בתוך מיכל לבן עגול: 🔍 (חיפוש רגיל) / ✨ (חיפוש חכם AI)
  - **בצד שמאל:** כפתור `+` עגול, רקע `amber-800`, אייקון לבן — הוספה לתיבת הכניסה

**התנהגות:** הקלדה + Enter → הטקסט נשלח ל-AI לסיווג אוטומטי לתחום/יעד, ואם רמת הביטחון גבוהה
הפריט נשמר לבד ומופיעה הודעת אישור עם אפשרות "ביטול"/"עריכה". אחרת — יורד לתיבת הכניסה.
מתחת לשורה עשויים להופיע פאנלים דינמיים: תוצאות תואמות, שאלת הבהרה, או כרטיס אישור.

### 4.3 סקשן "היום בעבודה"
- **כותרת סקשן:** "היום בעבודה" — 14px, `font-semibold`, `stone-500` (אפור, לא שחור), margin-bottom 12px.
- **תוכן:** רשימה קומפקטית של **עד 5 פריטים**, מופרדים בקווים אופקיים דקים מאוד.

**כל שורה (מימין לשמאל):**
| חלק | עיצוב |
|---|---|
| נקודה | 6×6, עגולה, `amber-800` |
| תווית זמן | 12px, `stone-400`, רוחב קבוע 64px — `"היום"` / `"מחר"` / `"אתמול"` / `"לפני N ימים"` / אות היום `"ג׳"`, ואחריה אופציונלי `" · 14:00"` |
| כותרת הפריט | 14px, charcoal, חיתוך בסוף השורה (truncate) |
| פעולות (ב-hover בלבד) | `✓` סימון כהושלם, `✎` עריכה — ריבועים 24×24 |

- **מיון:** 5 הפריטים הקרובים ביותר להיום (כולל פריטים **באיחור**), ואז מסודרים כרונולוגית.
- **מצב ריק:** טקסט ממורכז, 14px, `stone-400`: `"אין כרגע כלום קרוב"`

### 4.4 גריד הקטגוריות — לב המסך
גריד: **2 עמודות** במובייל · **3** ב-`sm` · **4** ב-`lg`. מרווח 12–16px.

**כל כרטיס — ריבוע מושלם (`aspect-square`), `rounded-3xl`:**
- רקע: **תמונת צילום** ממלאת את כל הכרטיס (`object-cover`), מתקרבת ב-105% ב-hover.
- אם התמונה חסרה → fallback: `linear-gradient(160deg, #92400E, #1C1A18)` + אייקון קווי לבן 32×32 במרכז.
- שכבת גרדיאנט כהה **רק בחמישית התחתונה** (`h-2/5`): `from-black/85 via-black/35 to-transparent`
  — כדי שהתמונה תישאר גלויה ברובה.
- בלוק טקסט לבן בתחתית (padding 12–16px), שלוש שורות:
  1. אייקון קווי 14×14 + **שם הקטגוריה** (13–14px, `font-semibold`)
  2. **תיאור** (10–11px, לבן 75%)
  3. קו accent קטן (20×3px, `rounded-full`, `#92400E`) + **מספר/סטטוס** (10–11px, לבן 90%)

**ששת הכרטיסים — לפי הסדר (הראשון = ימין-עליון):**

| # | שם | תיאור | הנתון המוצג | יעד | אייקון | תמונה |
|---|---|---|---|---|---|---|
| 1 | **קידום מותגים** | מותגים, תוכן מותגי וקמפיינים | `"{N} פתוחות"` | `/work/brands` | תגית | `brands.jpg` ✅ |
| 2 | **רעיונות וכתיבת תסריטים** | רעיונות לתוכן, Reels ותסריטים | `"{N} רעיונות"` או `"עדיין אין רעיונות"` | `/work/ideas` | נורה | `content-ideas.jpg` ❌ **חסרה** |
| 3 | **יבוא ומשלוחים** | ספקים, הזמנות ומשלוחים | `"{N} פעילים"` | `/work/shipments` | משאית | `shipments.jpg` ✅ |
| 4 | **משפיענים** | יוצרי תוכן ושיתופי פעולה | `"{N} פעילים"` | `/work/influencers` | אנשים | `collaborations.jpg` ✅ |
| 5 | **ניהול קמפיינים** | קמפיינים ממומנים ברשתות | `"{N} פעילים"` | `/work/campaigns` | מגפון | `campaigns.jpg` ✅ |
| 6 | **נוספים** | משימות עבודה כלליות | `"{N} פתוחות"` | `/work/area/more` | צ'ק-ליסט | `more.jpg` ❌ **חסרה** |

### 4.5 יומן עבודה
פאנל לבן `rounded-3xl`, padding 20px. מציג **רק פריטים של תחום עבודה** — אותם אובייקטים
שמופיעים גם ביומן הכללי (אין כפילות).

- **כותרת:** "יומן עבודה" (14px, `stone-500`) + בצד שמאל ניווט חודש: `→` · "אוגוסט 2026" · `←`
- **שורת ימים:** א ב ג ד ה ו ש (11px, `stone-400`)
- **רשת חודשית מלאה:** 42 תאים (6 שורות × 7), כל תא ריבועי, `rounded-lg`
  - מספר היום 10px; **היום הנוכחי** = עיגול מלא 16×16 ב-`amber-800` עם מספר לבן
  - יום נבחר = גבול `amber-400` + רקע `amber-50`
  - עד 3 נקודות זעירות (4×4, `amber-800`) לפי מספר הפריטים באותו יום
  - ימים מחוץ לחודש: שקיפות 30%
- **מתחת לרשת:** רשימת הפריטים של היום הנבחר — כותרת מימין, אייקון סוג + שעה משמאל
- **מצב ריק:** `"אין כלום ב-2026-08-25"`

---

## 5. מאיפה מגיע כל מספר (לוגיקה אמיתית)

| כרטיס | חישוב |
|---|---|
| קידום מותגים | פריטי עבודה פעילים שיש להם `brandId` |
| רעיונות | פריטים במאגר הרעיונות שאינם בארכיון |
| יבוא ומשלוחים | משלוחים שסטטוסם אינו "נמסר" |
| משפיענים | משפיענים בסטטוס "פעיל" |
| קמפיינים | קמפיינים בסטטוס "פעיל" |
| נוספים | פריטי עבודה פעילים **ללא** מותג ו**ללא** שם איש קשר, שהטקסט שלהם אינו מכיל: משלוח / ספק / יבוא / DHL |

"פעיל" = הסטטוס אינו `הושלם` ואינו `בוטל`.

---

## 6. מצבי קצה שחייבים להיות מעוצבים

- **מצב ריק מוחלט** (משתמש חדש): כל 6 המספרים = 0, "היום בעבודה" ריק, היומן ריק.
- **מספרים דו-ספרתיים** וכותרות ארוכות מאוד — חייבים להיחתך יפה ולא לשבור פריסה.
- **מצב כהה** — חובה. כל צבע חייב גרסה כהה.
- **RTL** — הכל מימין לשמאל. שים לב: **הילד הראשון בשורה מופיע הכי ימינה.**
- **מובייל 375px** — גריד יורד ל-2 עמודות, סרגל תחתון קבוע, כפתור צף `+`.

---

## 7. אילוצים — מה אסור

1. אין לשנות את ה-accent `#92400E` לצבע אחר.
2. אין להמציא נתונים, גרפים או KPI שאין להם מקור אמיתי במערכת (ראה טבלה בסעיף 5).
3. אין להוסיף אווטאר/שם משתמש/פרופיל — אין מערכת משתמשים.
4. הפונט חייב להישאר Rubik.
5. הסקשנים והיעדים (6 הכרטיסים והראוטים שלהם) חייבים להישמר — זו מפת הניווט של האזור.

---

## 8. חולשות ידועות של העיצוב הנוכחי — כאן בדיוק צריך שיפור

1. **אין שורת סיכום/KPI בראש** — נכנסים ולא רואים מיד "כמה דחוף היום", "מה באיחור".
   (באזור הלימודים כן קיימת שורה כזו ועובדת טוב.)
2. **שתי תמונות חסרות** (`content-ideas.jpg`, `more.jpg`) → שני כרטיסים נראים שונה מהשאר.
3. **כל 6 הכרטיסים באותו צבע** — אין הפרדה ויזואלית מהירה בין תחומים.
4. **הנתון בכרטיס הוא מחרוזת שטוחה** ("3 פעילים") — אין סימון דחיפות, איחור או מגמה.
5. **אין שדה חיפוש** בדף.
6. **"נוספים" מוביל לעמוד "בקרוב"** ולא למערכת אמיתית.
7. **ריבועים מושלמים (aspect-square) בגריד 4** תופסים הרבה גובה ודוחפים את היומן הרחק למטה.
8. **היומן החודשי המלא** בתחתית גדול יחסית לערך שהוא נותן בכניסה.

---

## 9. המטרה של העיצוב החדש

מסך שבשנייה אחת עונה על:
1. מה דחוף לי **היום** בעבודה?
2. איפה יש **חריגה** (משלוח תקוע, קמפיין שנגמר, מותג שמחכה)?
3. לאן אני נכנס עכשיו?

סגנון: **צעיר, נקי, פרימיום, מובן מיד** — לא דשבורד אדמיני ולא רשימת משימות עטופה בכרטיסים.
הרבה whitespace, היררכיה דרך גודל טיפוגרפי ומספרים גדולים, ולא דרך בלוקים כבדים.
